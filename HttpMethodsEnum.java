/**
 * This can be used to enforce that all HTTP methods are handled in the
 * {@code switch} statement inside {@code HttpRequestHandler}
 **/
public enum HttpMethodsEnum {
    GET, POST, PUT, DELETE
}
